import React from "react";
import ListVisibleAnimation from "./ListVisibleAnimation";

const ListViewableItemAnim = () => {
  return <ListVisibleAnimation type="rotate-blur" />;
};

export default ListViewableItemAnim;
